
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: scripts.tests.kubtest.io
spec:
  group: tests.kubtest.io
  names:
    kind: Script
    listKind: ScriptList
    plural: scripts
    singular: script
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Script is the Schema for the scripts API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ScriptSpec defines the desired state of Script
            properties:
              content:
                description: script content - executor specific e.g. fo postman-collections
                  executor
                type: string
              git-branch:
                description: git branch/tag name for checkout
                type: string
              git-directory:
                description: if needed we can checkout particular directory in case
                  of BIG/mono repositories
                type: string
              input-type:
                description: 'script content type can be:  - direct content - created
                  from file,  - git repo directory checkout in case when test is some
                  kind of project or have more than one file,  - or file uri'
                type: string
              type:
                description: 'Important: Run "make" to regenerate code after modifying
                  this file script type - what executor type should be used during
                  test execution'
                type: string
              uri:
                description: uri of content file or git directory
                type: string
            type: object
          status:
            description: ScriptStatus defines the observed state of Script
            properties:
              executions_count:
                type: integer
              last_execution:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
