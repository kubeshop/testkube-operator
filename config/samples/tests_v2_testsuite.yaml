apiVersion: tests.testkube.io/v2
kind: TestSuite
metadata:
  name: testsuite-sample
spec:
  steps: 

    - type: delay
      delay: 
        duration: 1000

    - type: executeTest 
      execute: 
        namespace: testkube 
        name: sanity
